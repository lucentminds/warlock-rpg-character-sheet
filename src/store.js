import { createStore } from 'vuex';
// import RequestError from '@/lib/request-error';

export default createStore({
   state: {
   },// /state{}

   mutations: {
   },// /mutations{}

   actions: {
   },// /actions{}

   getters: {
   },// /getters{}
});
